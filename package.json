{
  "name": "audiobook-translator",
  "version": "1.0.0",
  "description": "Multi-language Text-to-Speech Translator with OpenAI TTS",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && mkdir -p backend/public && cp -r frontend/dist/* backend/public/",
    "start": "cd backend && npm install && npm start",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "tts",
    "translation",
    "openai",
    "multilanguage",
    "audiobook",
    "text-to-speech"
  ],
  "author": "Ishita Majumdar",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}